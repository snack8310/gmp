# AGENTS 指南

本文件是本仓库智能体协作的**唯一规则真源**，定义 AI 与人在本仓库中的输入输出、边界、责任划分和验收标准。

`CLAUDE.md`、`.claude/`、`.cursor/` 等工具私有文件只作为各自工具的会话入口，不作为团队共享真源；如与本文件冲突，以本文件为准。

## 规则优先级

当多个来源的规则同时存在或出现冲突时，按以下优先级执行（从高到低）：

1. 平台与系统级安全 / 合规规则（不可覆盖）。
2. 用户在当前会话中的明确指令（不得违反第 1 条）。
3. 仓库根目录 `AGENTS.md`（全仓库默认规则）。
4. 子目录 `AGENTS.md`（仅对该目录及其子树生效；在其作用域内优先于根规则的同类细则）。
5. 其他参考文档（`README.md`、`docs/`、历史设计文档）。

### 作用域与冲突处理

- 根目录 `AGENTS.md` 对整个仓库生效。
- 子目录 `AGENTS.md` 仅影响该子树，不跨目录生效。
- 同时命中多份 `AGENTS.md` 时，遵循「全局规则先满足、局部规则再细化」的原则。
- 若局部规则与全局规则冲突，且不违反第 1、2 条时，以更具体路径（更近目录）的规则为准。
- 改动落在某子目录时，必须同时读取该目录的 `AGENTS.md`。

## 全局约束

- **ALWAYS RESPONSE IN CHINESE**
- **OUTPUT DOCUMENTS WRITE IN CHINESE**
- **CODE COMMENTS WRITE IN ENGLISH**
- **GIT COMMIT MESSAGE WRITE IN ENGLISH**
- 不直接下发生产变更或删除操作；若需执行，先生成变更计划与审批要点。
- 避免泄露或索要敏感信息（个人信息、密钥、凭据、令牌）。
- 对外部依赖（数据库、缓存、消息队列、配置文件、第三方服务等）的改动，需显式标注读 / 写影响面。
- 所有与外部系统的交互都需打印结构化日志，且不得输出密钥、凭据、令牌或个人敏感信息。
- 当信息不足时，列出所需补充的输入，而非猜测。仓库无法证明的配置、路径、字段名、默认值不得臆造，应改为必填并在执行前失败。
- 当你学习到项目的新规范时，建议更新本指南；如有必要，同时建议更新 `README.md`。

## 约束落地优先级（元规则）

同一条约束，能用更早的手段固化就不要退到更晚的手段：

1. **类型与接口**：让违反约束的写法编译不过、或在 API 边界就无法表达。
2. **自动化检查**：单测、契约测试、lint、CI 门禁。
3. **规则文档**：写进 `AGENTS.md` 或子目录 `AGENTS.md`。
4. **人工评审**：只兜住前三层确实覆盖不了的部分。

散文体的「禁止 / 必须」是最后手段，不是第一手段。每次准备新增一条规则时，必须先回答：这条能不能变成一个类型约束或一个必跑的测试？如果能而没做，应说明原因。

引入外部项目的经验时同样适用：借鉴其**方法与判断标准**，但对方沉淀的具体历史约束，优先在本仓库的设计阶段消除，而不是复制成规则。

## AI Loop Native 约束

- 本仓库采用**半自动研发执行模式**：AI 可以读仓库、改代码、补测试、跑验证、生成交付说明，但**不默认自动 merge**，由人工最终放行。
- 工作模式采用「**主 Agent + 评估器双环**」：主 Agent 负责实施，评估器负责基于显式证据裁决是否达标；**主 Agent 不得自行宣布完成**。
- 评估器必须与主 Agent **物理隔离**：
  - 使用独立的只读评估器承担审查角色，禁止由主 Agent 自我裁决。
  - 评估器输入仅限任务卡 + 实现代码 / diff + 必要证据与上下文，**不携带主 Agent 的实现过程和心路历程**。
  - 只给只读工具权限，禁止评估器直接改代码。
  - 评估器裁决**不可被主 Agent 覆盖**。
- 评估器调用默认使用 [评估器协议模板](docs/ai-loop/evaluator-protocol-template.md) 组织输入与输出；结论必须为 `PASS` / `FAIL` / `NEEDS-EVIDENCE` 之一，并列出阻断问题、非阻断风险、缺失验证、范围与规则符合性、抽象记忆建议和证据。
- 评估器的工作方法论是**对抗性**的：默认假设实现存在问题，主动寻找反例、边界条件、未覆盖场景，只有找不到问题时才判定通过，而不是逐项打勾式确认。对抗不等于挑刺：每条质疑**先立最强版本再攻击**，结论写成「当……时会……」的可证伪表述，并按 **影响 × 出错概率 × 验证成本** 排序。
- **取证纪律**：评估器每条阻断问题必须同时引用**声明侧**原文（任务卡验收标准、规格文档、接口契约、设计文档、历史记忆）和**实现侧** `文件:行号`；两侧引不全的只能记为「缺失验证」，不能记为阻断问题。**声明了但未实现，本身即缺陷。**
- **第一性原理贯穿三个环节**：任务卡阶段拆解表面需求、识别真实问题与硬约束；方案设计阶段验证每个候选方案是否有更本质的解法，不直接套用惯例；评估器审查阶段核对是否真正解决了任务卡里的本质问题，而不是只看实现「看起来对不对」。
- 进入文件修改前，主 Agent 必须按 [任务入口检查清单](docs/ai-loop/task-intake-checklist.md) 完成入口检查，明确任务类型、等级、高风险项、人工确认、历史规则命中、最小验证集和评估器重点；检查结果应写入任务卡，或在小任务中以可追溯形式进入评估器输入包。
- 日常输入允许自然语言，但进入实施前必须整理成**结构化任务卡**；未完成任务卡整理，不进入代码修改阶段。
- 任务分为新需求开发、缺陷修复、重构优化、文档治理；按小任务 / 大任务分级治理，不一刀切。

### 任务卡要求

- **小任务**至少包含：标题、类型、背景、目标、范围、验收标准、风险点。
- **大任务**至少包含：标题、类型、背景、目标、范围、非目标、验收标准、影响面、风险点、验证方案。
- **风险点必须按三档写**，不能只列平铺清单：
  - **真风险**：确实可能出问题，再分 阻断 / 放行后续跟进 / 仅观察。
  - **伪风险**：表面像问题但判断不成立，必须写明为何不投入。
  - **未讨论项**：不确定是不是问题、且当前没人在看的认知盲区，必须列成澄清问题或交给评估器重点攻击。

判定标准见 [任务入口检查清单](docs/ai-loop/task-intake-checklist.md)。

### 分级执行规则

- **小任务**：代码通过 + 相关回归通过即可进入人工最终审核；最多允许 **2 轮**自修复；默认按「研发集」标准验收。
- **大任务**：代码通过 + 回归通过 + 人审确认后才算完成；实施前和最终都需人工审核；最多允许 **3 轮**自修复；默认按「工程集」标准验收。
- 超出自修复轮次上限仍未通过评估器的，停止自修复，输出当前状态、失败原因和已知限制，交人工裁决。

### 高风险操作额外卡点

命中高风险项的改动，必须**额外人工确认**后才能推进。

**判定标准**（满足任一即为高风险）：

1. 变更核心领域模型的语义、状态机或生命周期。
2. 变更被其他模块或外部调用方依赖的接口契约、协议或数据格式。
3. 变更幂等性、去重、重试、并发或执行顺序相关的逻辑。
4. 变更持久化结构、涉及数据迁移或数据回填。
5. 变更权限、认证、审计或安全边界。
6. 变更同一能力的多个实现之间必须保持一致的行为（例如同一接口的不同后端实现）。
7. 影响范围跨多个模块 / 服务，或错误后果不可通过重跑自愈。

**项目高风险清单**（随项目演进维护，新增核心能力时必须同步补充）：

| # | 高风险项 | 判定标准命中 | 错了会怎样 |
|---|---|---|---|
| 1 | 变更画布 DSL、节点与连线的执行语义 | 1, 2, 7 | 画布是所有能力的编排底座。DSL 语义一旦有歧义或被悄悄改动，其上所有组件的行为都跟着漂，且已保存的画布可能被重新解释成另一个意思 |
| 2 | 变更组件契约（输入 / 输出 / 配置 / 幂等 / 重试）或组件注册表语义 | 1, 2, 3, 6, 7 | 触达与权益都只是组件的一种实现，共用同一套契约。契约变更会同时影响全部实现，且不兼容的组件可能直到运行时才暴露 |
| 3 | 变更画布运行实例的状态机与状态流转 | 1, 3, 7 | 运行实例状态错乱无法靠重跑自愈：可能重复执行已完成的节点，也可能让流程永久卡死在中间态 |
| 4 | 变更用户名单的来源接入、圈选、快照或生命周期 | 1, 2, 4, 7 | 这四者共同决定「这批人到底是谁」。错了就是对错误人群执行，且已经发出的触达不可撤回 |
| 5 | 变更 A/B 分流单元、分桶算法、实验定义或指标口径 | 1, 3, 6, 7 | 分桶必须稳定且可复现。这四者中任一项变更都会让历史实验结论失效，且通常无法回算 —— 错误的结论会继续指导后续决策 |
| 6 | 变更触达模式与权益模式共用的统一执行契约 | 1, 2, 6, 7 | 这是本项目的核心抽象目标本身。任一模式的特殊需求侵入公共契约，抽象即塌陷，且塌陷是渐进的、不会有明确的报错时刻 |
| 7 | 变更权益侧的额度、库存与发放逻辑 | 1, 3, 7 | 超发是**总量控制**失效，不是重复发送 —— 幂等与去重一次都不会命中。已发放的权益通常不可回收 |
| 8 | 变更触达资格判定（退订、免打扰时段、频次疲劳） | 1, 3, 7 | 向已退订用户发送是**资格判定**失效，同样不是幂等问题。这类判定失效不会报错，只会安静地把消息发出去 |
| 9 | 变更渠道连接器协议或发送结果回执的关联方式 | 2, 3, 6, 7 | 对接外部渠道，发送动作不可撤回。回执关联错误会导致重复发送，或让执行状态永远悬空 |
| 10 | 变更执行态存储实现，或多个执行态实现之间必须保持一致的行为 | 3, 4, 6, 7 | 多个实现共用同一执行接口，但行为可能悄悄分叉：测试在一种实现下全绿，线上跑的是另一种。这是本项目最典型的「静默失效」风险面 |
| 11 | 变更幂等键、去重、重试或限流策略 | 3, 7 | 触达类系统最贵的错误是重复发送。幂等边界一旦松动，故障恢复动作本身会把小故障放大成事故 |
| 12 | 迁移或回填已产生执行记录的数据（名单快照、运行实例、发送回执） | 1, 4, 7 | 这类数据的「重跑」不是幂等的：回填可能把已完成的流程重新拉起，或让已发出的触达再发一次。与迁移一张静态配置表是两回事 |
| 13 | 变更执行动作的授权边界或不可否认性（谁能对哪批人群发起执行、发布由谁批准） | 1, 5, 7 | 触达是对真实用户的不可撤回动作。授权边界松动会让误发无法被拦下，留痕缺失则事后无法确认是谁发起的 |

清单依据当前业务主线（用户名单 → 流程画布 → 多渠道触达 → 组件 / 实验 / 权益抽象）与技术主线（存储为主，执行态后续演进到缓存实现）拟定，描述的是**风险边界**而非设计。

本表列的是**通用判定标准推导不出来、必须靠领域知识才能识别**的风险项。7 条通用判定标准始终独立生效：一项改动即使不在本表中，只要命中任一条标准，同样属于高风险。本表的作用是免去现场推导，不是高风险的唯一入口。

本表与 7 条通用判定标准中的「变更」**均涵盖「首次引入该能力」**。本项目处于从零建设阶段，若把「变更」读成只针对既有实现，整表会在风险最高的阶段被整体判为不适用。

本表条目名称沿用当前业务主线的措辞，**不构成设计约束**。当条目措辞与领域设计文档冲突时，以设计文档为准，并同步修订本表 —— 不得因为根 `AGENTS.md` 在规则优先级上高于 `docs/`，就用本表反过来锁死设计。

核心抽象落地或调整时，必须复审本表，**包括卡点密度**：核心抽象未落地阶段几乎每个任务都命中高风险是正确结果；抽象稳定后做增量功能时若仍然全命中，说明清单需要收窄，否则人工确认会退化成橡皮图章。

技术栈尚未确定，因此本表只覆盖领域语义与执行语义层面的风险，不含具体技术栈相关风险；技术栈确定后需重新评估是否补充。

只要任一高风险项为「是」，主 Agent 必须：

1. 在任务卡中明确标注高风险项。
2. 实施前列出需要人工确认的问题。
3. 将相关规格、历史记忆、验证方案放入评估器输入包。
4. 在变更证据中记录风险处理方式、验证结果和未覆盖项。

如缺少人工确认或验证证据，评估器应输出 `FAIL` 或 `NEEDS-EVIDENCE`。

### 破坏性变更

对外公开的接口、协议、配置格式、数据格式的**破坏性变更**，优先级高于普通高风险项，额外要求：

- 在任务卡中显式标注为破坏性变更，并说明受影响的调用方范围。
- 给出版本化策略、兼容期与废弃计划，或明确说明为何不需要兼容。
- 变更必须进入 `CHANGELOG`（或等价的对外变更记录）。
- 不得以「内部还没有人用」为由跳过上述要求，除非在任务卡中由人工确认。

## 评估与验证

- **研发集**至少包括：编译 / 构建通过、单元测试通过、相关集成测试或接口验证通过、变更说明完整。
- **工程集**至少包括：满足研发集全部要求、格式化与静态检查通过、关键路径覆盖充分、风险点说明完整、回滚说明完整。
- 评估结论必须基于命令结果、测试结果和显式证据，**不得仅基于模型主观判断**。
- **验证的成功凭据必须来自真实执行结果**，不得以流程编排工具的界面状态、任务依赖关系或「上一步没报错」推断成功。
- 因沙箱、网络、权限限制无法执行的验证，**不得视为通过**；应记为缺失验证并说明替代证据。

## 记忆与沉淀

### 单任务循环内的读写时机

- **任务卡整理前**：主 Agent 读 `docs/history/memory/`（长期规则）+ 近期同领域 `docs/history/changes/`。
- **评估器审查通过、进入人工最终审核前**：主 Agent 写一份 `docs/history/changes/`（背景、关键实现、关键文件、验证结果、风险与未覆盖项）。
- **评估器审查通过后**：由**评估器**（独立视角）判断本次任务是否暴露长期有效的规则、踩过的坑或关键权衡，如有则写入 `docs/history/memory/`。该判断不由主 Agent 作出。

### 沉淀目录约定

- `docs/history/changes/`：单次需求 / 缺陷 / 重构的**变更证据**。写事实，不写长报告。
- `docs/history/memory/`：长期有效的**抽象记忆**。写稳定规则，不写过程流水账。
- 具体格式见 [历史沉淀说明](docs/history/README.md)。

新增抽象记忆前，先按「约束落地优先级」判断该规则能否被类型或测试固化；只有确实无法固化的部分才写成记忆。

### 上下文加载顺序

AI 默认按以下顺序建立上下文：

`AGENTS.md` → `README.md` → `docs/history/memory/` → 当前相关规格或功能文档 → `docs/history/changes/` → 相关代码与测试。

涉及子目录改动时，在读完根 `AGENTS.md` 后立即读对应子目录的 `AGENTS.md`。

### 测试基线

- 测试基线用于定义「这次改动**至少要证明什么没有坏**」，不允许只跑无关测试后直接宣布完成。
- 每次完成代码类需求后，至少沉淀一份变更证据。
- 当形成长期有效的实现约定、关键权衡或历史坑时，必须补充抽象记忆，而不是只保留需求原文。

## 协作流程与分支约定

### 硬规则

- **禁止直接向 `main` 提交或推送。** 所有变更必须走「issue → 分支 → PR → 人工放行」。
- **每个 PR 必须关联一个 issue**（`Closes #N`）。唯一例外：仓库初始化与协作规范自身的引导提交。
- **AI 不得自行合并 PR。** 合并动作由人工执行。
- PR 必须填完 `.github/pull_request_template.md`，其中评估器结论为 `PASS` 是进入人工审核的前置条件。

### Issue 规范

issue 是任务卡的**输入**，不是任务卡本身。模板位于 `.github/ISSUE_TEMPLATE/`，三类对应本文件定义的任务类型：

| 模板 | 对应任务类型 | 默认标签 |
|---|---|---|
| `bug_report.yml` | 缺陷修复 | `type/bug` |
| `feature_request.yml` | 新需求开发 | `type/feature` |
| `chore.yml` | 重构优化 / 文档治理 | `type/chore` |

标签约定：

- 类型：`type/bug`、`type/feature`、`type/chore`
- 等级：`level/small`、`level/large`
- 风险：`risk/high`（命中高风险判定标准）、`risk/breaking`（对外破坏性变更）
- 状态：`status/needs-clarification`（存在未澄清的未讨论项，不进入实施）

issue 中的「是否可能命中高风险项」选「是」或「不确定」时，实施前必须由人工确认并打上或摘掉 `risk/high`，不得由 AI 单方面判定为否。

### 分支命名

```
<type>/<issue 编号>-<短横线主题>
```

`<type>` 取值：`feat`、`fix`、`refactor`、`docs`、`chore`。示例：`fix/34-duplicate-delivery-on-retry`。

引导提交（无关联 issue）可省略编号段。

### Commit 规范

采用 Conventional Commits，**英文书写**：

```
<type>(<scope>): <subject>

<body>

Refs #<issue 编号>
```

- `<type>` 与分支类型一致。
- subject 用祈使句、不超过 72 字符、结尾不加句号。
- body 说明**为什么**这么改，而不是复述改了什么。
- 破坏性变更必须使用 `!`（如 `feat(api)!: ...`）并在 body 中说明影响范围。

### PR 规范

- 标题与分支主题一致，可用中文。
- 一个 PR 只解决一个 issue；范围漂移应拆成新 issue，而不是扩大当前 PR。
- 合并方式统一 squash，合并后删除分支。
- 命中 `risk/high` 或 `risk/breaking` 的 PR，人工审核不可省略，不适用任何快速通道。

### 与 AI Loop 的衔接

| 阶段 | 产出 | 落点 |
|---|---|---|
| 需求进入 | issue | GitHub Issue |
| 入口检查 | 任务卡 | 任务卡文件或会话内可追溯记录 |
| 实施 | 代码 + 测试 | 特性分支 |
| 评估器审查 | `PASS` / `FAIL` / `NEEDS-EVIDENCE` | PR 正文 |
| 变更证据 | 事实记录 | `docs/history/changes/` |
| 抽象记忆 | 长期规则 | `docs/history/memory/`（由评估器判断是否新增） |
| 人工放行 | 合并 | 人工执行 |

评估器结论非 `PASS` 时，PR 不得请求人工审核；应先补齐证据或修复问题，且受分级执行规则的自修复轮次上限约束。

## 边界与外泄

- **对外发布边界**：仓库中不得出现内部主机名、内部镜像仓库地址、内部服务名与命名空间、内部工单号、真实客户或业务方名称、任何形式的凭据与令牌。此约束应尽可能由自动检查保证，而不只依赖人工自觉。
- **跨仓库复制完整性**：可以借鉴其他仓库的方法论与判断标准，但**不得直接搬运其实现代码**。任何来源于其他仓库的代码片段、配置或文档，必须在任务卡中显式声明来源与授权状态，并由人工确认后才能进入本仓库。
- **示例与文档中的数据**：文档、测试、示例中使用的数据必须是构造数据，不得使用真实用户数据或真实业务数据。
