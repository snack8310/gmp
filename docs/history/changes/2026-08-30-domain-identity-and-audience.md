# 建立领域规格真源并定义用户身份与用户名单

关联：issue #10

## 背景

仓库已有完整治理骨架（`AGENTS.md`、AI Loop 双环、shell 门禁），但**领域侧一份设计文档都没有**。唯一描述业务的载体是 `AGENTS.md` 的 13 条高风险清单——那是一张失效边界表，不是模型。

真正要解决的问题不是「开始做业务模型」，而是：**同一套领域概念即将被多处实现，而它目前只存在于对话里。没有可引用的规格真源时，跨实现的语义漂移不报错，只会安静分叉**——正是高风险清单第 10 条描述的失效模式。

`AGENTS.md` 早已写明「以设计文档为准」，但该文档在仓库中无约定路径。这一缺口被 `2026-08-30-high-risk-inventory.md` 记为真风险（仅观察），本次承接。

第一个模型选「用户身份 + 用户名单」，依据是依赖方向与可补救性，不是重要性：它是唯一不依赖下游的模型，且定错的修复手段是数据迁移与回填（高风险第 12 条），是本主线里唯一不能靠重跑自愈的一类。

**已考虑并否决的更本质解法**：先写代码、让模型从实现里长出来。否决理由是核心抽象（高风险第 6 条）的塌陷是渐进的、没有明确报错时刻，先实现后抽象在本项目上恰好是最危险的顺序。用户已确认采纳「先文档后代码」。

## 关键实现

### `docs/domain/` 的定位与写法约定

- 确立 `docs/domain/` 为领域语义真源，与 `AGENTS.md`（协作规则真源）管辖范围不重叠。
- 规定每份领域文档的必备部分：概念定义、不变量、状态机、引用纪律、边界与非目标、待澄清问题清单。
- **每条不变量必须标注目标固化层级**（`类型` / `检查` / `文档`），实现仓库落地时必须把前两类下沉，仍留在文档层的属已知欠账。
- 规定状态机必须写成状态取值表 + 流转矩阵，**禁止散文排除句**；规定「检查」层不变量的断言中每一项都必须在同一文档内可取值。
- 领域文档不得出现语言、框架、存储、表结构、序列化格式、目录结构。

### 用户身份

- **Subject 由平台自签发的 `uid` 唯一确定，`uid` 不可变、不复用**；Identifier 是 `(域, 值)` 二元组，Subject 1:N Identifier。
- 主标识即 `uid`。名单成员、分桶输入、幂等键、执行记录、回执关联一律使用 `uid`（I3、I4）。Identifier 只出现在入站解析与出站解析两处。
- 入站解析的失败必须是显式结果，不得回落为「跳过该条」（I5）。

### 用户名单

- **AudienceDefinition（可变的圈选规则）与 AudienceSnapshot（不可变的物化结果）拆为两个概念**，任何执行只能绑定快照（A4）。
- 「动态名单」表达为「同一定义被反复物化出多个快照」，因此与「快照不可变」不冲突——变的是快照个数，不是快照内容。
- 引用纪律：其他上下文只允许持有快照标识、规模、物化时点，**禁止**持有圈选规则、来源配置、定义标识。即「知道这批人是哪批，不知道怎么选出来的」。
- 名单是独立限界上下文，只定语义边界，不定服务拆分。

### 逐项承接高风险第 4 条

第 4 条的四个分项——来源接入、圈选、快照、生命周期——各自独立成节，并以**分项承接表**逐项映射到章节。以来源侧为遍历基准编制，无合并、无复合词压缩、无「判定不适用」项。见 [来源枚举保真规则](../memory/source-enumeration-fidelity.md)。

### 回填

`AGENTS.md` 第 123 行的「以设计文档为准」回填为可解析路径 `docs/domain/README.md`。

## 关键文件

- `docs/domain/README.md`：新增。领域规格目录定位、文档写法约定、固化层级规则、自动化检查清单、变更规则、索引。
- `docs/domain/identity-and-audience.md`：新增。第一份领域模型。
- `AGENTS.md`：改动一行（第 123 行），纯路径回填，清单 13 条内容未动。

## 人工确认记录

本任务为**大任务** + `risk/high`，`AGENTS.md` 要求实施前人工确认。issue #10 列出 5 条未讨论项，实施前逐条呈请用户确认，答复如下（均为用户直接答复，非推断）：

| # | 未讨论项 | 状态 | 答复 | 落到文档何处 |
|---|---|---|---|---|
| 1 | 主标识选型 | **已确认** | 平台内部 uid 为主体，外部标识作为其下的 Identifier | 1.1–1.4 |
| 2 | 名单快照语义 | **已确认** | 拆成定义 + 快照两个概念 | 3.3、3.4、4.1 |
| 3 | 身份变更追溯语义 | **已确认（排除该场景）** | 「不用关心身份变更逻辑，这个场景不存在」 | 2.2、7 |
| 4 | 名单归属边界 | **已确认** | 独立限界上下文 | 4.3 |
| 5 | 规格真源漂移检测 | **未确认** | 未讨论 | 记入本文未覆盖项 |

另有三项会话内确认：任务顺序（先 #10 再 gmp-core）、gmp-core 定位（本仓库内的核心领域模块）、技术栈（**Go**）。

**技术栈决策未在本次落地**：issue #10 硬约束 1 明令本轮不引入技术栈相关设计，`AGENTS.md` 第 129 行「技术栈尚未确定」仍待另行送审修订。按 `AGENTS.md`「范围漂移应拆成新 issue」处理。

## 验证结果

| 命令 / 检查 | 结果 | 证据摘要 |
|---|---|---|
| `sh test/githooks/pre-push_test.sh` | 通过 | `Shells run: sh bash dash zsh ksh`；`Assertions: 105`；`OK` |
| 文档一致性检查脚本 | 通过，失败 0 | 断言类别：相对链接与页内锚点可解析、记忆引用有对应文件、承接表以 `AGENTS.md` 第 4 条为**来源侧基准解析**（非硬编码）逐项承接、承接表指向的锚点存在、无技术栈泄漏、回填路径存在、必备章节齐备、不变量固化层级取值合法、不变量编号连续且被引用者存在、状态机小节全部被发现、状态取值表含终态标注、流转矩阵覆盖全部有序状态对且取值合法。**不记断言计数**——它是工具产生的量，写进送审载体只会失真 |
| 对检查脚本的变异测试 | 全部被捕获 | 引用完整性组：承接表锚点改错、删掉「快照」承接行、回填路径指向不存在文件、固化层级写成 TODO、正文塞入存储实现名、制造无对应文件的记忆引用。状态机结构组：流转格取值改「未定义」、删矩阵整行、去掉终态标注、对角格改允许、状态表新增状态不同步、矩阵行顺序错乱。空断言组（由复审评估器设计）：状态机小节改名、删除不变量整行、同行内既含否定词又含真实泄漏、来源侧新增分项 |
| 变异后还原完整性 | 通过 | 每次变异后立即还原，`git status` 无残留 |
| 独立评估器审查 | 首轮 **FAIL** → 复审 **FAIL** → 第三轮见 PR 正文 | 结论以 PR 正文记录的评估器原始输出为准 |
| 既有 CI 门禁 | 见 PR checks | 以本 PR 的 checks 为准（自动绑定 head SHA）。**不引用固定 run id**，见 [送审载体一致性](../memory/review-package-consistency.md) |

## 评估器首轮发现的两处不可判定语义

两条阻断问题的共同根因值得记录：**产物在形式上齐备（不变量表、固化层级列、状态机、承接表，5000+ 条机器断言全绿），而形式齐备恰好掩盖了语义不可判定。**

| # | 问题 | 修复 |
|---|---|---|
| 1 | 不变量 A5「来源条目数 = 成员数 + 拒绝集条目数」在「平台自有数据」这类来源下，左项无定义——该来源没有可枚举的输入清单。实现方只能在「把全平台主体计入拒绝集」与「对该来源放弃断言」之间自选，而后者让覆盖面最大的一类来源失去保护且不报错 | 3.1 新增「来源条目集」小节，逐类来源定义其是否存在；A5 限定为「对存在来源条目集的来源」，新增 A9 覆盖另一类；3.2 的拒绝原因按来源类型收敛 |
| 2 | 「不允许的流转：草稿 → 已归档以外的任何逆向流转」有两种读法（该流转是例外还是被禁止），与状态图、草稿态说明相互矛盾；快照的「已过期」未声明终态 | 两个状态机改写为状态取值表（逐个标注是否终态）+ 完整流转矩阵；「草稿 → 已归档」定调为允许（否则草稿态无终结路径）；显式定调「物化中 → 已过期」「就绪 → 已失败」两格 |

首轮评估器另指出：问题 2 的写法约束本可前置固化却写成了散文（原始结论见 PR 正文）。

另接受 3 条非阻断修正：收窄「Identifier 是可变的」的外延（避免被读作可解绑）、去掉固化层级注释中的实现术语、收窄承接表「快照」行的章节指向以消除说明列的覆盖缺口。

## 复审发现的四条阻断问题

复审（送审 `a7e75a1`）判定首轮阻断 2 已真正闭合、阻断 1 闭合主体，但发现四条新问题。它们的共同性质是**送审载体对自身的陈述不成立**——产物本身的领域语义没有方向性错误：

| # | 问题 | 修复 |
|---|---|---|
| 1 | `docs/domain/README.md` 写下「以下约定**已固化为自动化检查**」，而承载它的脚本未入库、CI 中不存在任何针对 `docs/domain/` 的检查。同一份变更证据在别处又写明「未入库、无回归防护」，自相矛盾。该文档定位为跨 repo 引用的真源，下游读者会以为写法违规会被挡下 | 改写为**约定的固化归属表**，逐条标注「可机检？」与「当前固化状态」，九条一律如实标**未固化**，并写明「当前没有任何针对本目录的自动化检查在 CI 中运行」。参照 `shell-script-portability.md` 已示范的形态 |
| 2 | 「已固化」声称表本身也不成立：「每个非终态必须存在通往终态的路径」脚本无任何可达性断言；必备章节断言只对单个硬编码文件路径生效且漏掉「引用纪律」。复审评估器另设计四组变异，证明脚本存在空断言路径：状态机小节改名后全部结构断言静默消失、删除不变量整行无存在性断言、技术栈检查按否定词整行跳过可被绕过、承接表分项硬编码而非从来源侧解析 | 四条空断言全部修复并逐条做变异验证；固化归属表中把「可达性断言」如实标为未实现 |
| 3 | 一个定义混合「平台自有数据 + 外部导入」两类来源时，A5 的左项再次无定义——首轮阻断 1 的同一失效模式在更窄条件下残留。根因是 A5/A9 按**来源**收敛，而等式右项是**快照级**的量，而文档从未声明一个定义只能有一类来源 | 3.1 新增「一个定义只有一个来源」并写明理由；多来源组合升为待澄清第 1 条，并预先指明届时必须同时定义拒绝集按来源分账 |
| 4 | 检查脚本的断言总数在三份载体中出现三个互不相同的数字，且无一等于送审 commit 上的实测值。任务卡却自称已命中「摘要不写计数」规则 | 送审载体全部改为枚举断言类别，不写任何工具产生的计数；检查脚本自身也不再输出计数 |

复审同时指出任务卡把 `shell-script-portability.md` 判为「不适用」**不成立**：该记忆的「未固化的部分仍是文档约定，不得声称为已固化」是跨语种通用规则，本次恰好违反。此项判定失误如实记录于此。

## 风险与未覆盖项

- **真风险（放行后续跟进）**：文档一致性检查脚本**未入库**，是一次性取证工具，对后续提交无回归防护。三轮评估器审查发现的全部阻断问题，都在人工阅读层被发现、被机器断言集漏过——现有断言只覆盖引用完整性与结构，不覆盖语义完备性。需开独立 issue 承接入库。
- **真风险（放行后续跟进）**：`docs/domain/` 与各实现仓库之间无任何漂移检测机制。issue #10 未讨论项 5，明确不解决。
- **真风险（仅观察）**：人工确认排除了「换号 / 注销 / 账号合并」整类场景。文档 2.2 已如实记录该边界及其代价（属破坏性变更，修复手段是迁移回填）。若该场景日后出现，必须重走完整高风险确认流程，不得当作增量补充。
- **真风险（仅观察）**：`AGENTS.md` 第 129 行仍写「技术栈尚未确定」，而用户已在会话中拍板 Go。两者暂不一致，需另行送审修订。
- **本次不覆盖项**：领域模型的长期业务正确性（后续轮次必然修订）；高风险清单卡点密度复审（核心抽象未落地，条件不满足）；待澄清问题清单中的 7 项，作为下一轮（组件契约 + 一次执行）的输入。
- **需要后续动作**：开 issue 承接检查脚本入库（含 `docs/domain/README.md` 固化归属表中标「未固化」的九条）；开 issue 修订 `AGENTS.md` 的技术栈表述。
- **抽象记忆**：复审评估器判定应新增 [[mechanization-claims]]，并上调 [[source-enumeration-fidelity]] 的计数规则由「尽量不写」为「禁止」。两者已按评估器给出的规则草案转录落盘。
