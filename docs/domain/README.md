# 领域设计文档

本目录是本项目**领域模型的规格真源**，供所有 repo 引用。

根 `AGENTS.md`「项目高风险清单」中「当条目措辞与领域设计文档冲突时，以设计文档为准」所指的「领域设计文档」，即本目录下的文档。

## 定位

- 本目录描述**领域语义**：概念、标识、不变量、状态机、边界。
- 本目录**不描述实现**：不含技术栈、语言、框架、存储结构、目录结构、接口签名。
- 各 repo 的实现必须符合本目录的语义；实现细节写在各自 repo 内，不写回本目录。

## 与其他文档的关系

| 文档 | 关系 |
|---|---|
| 根 [`AGENTS.md`](../../AGENTS.md) | 规则真源。规则优先级上高于 `docs/`，但**高风险清单的条目措辞不构成设计约束**，冲突时以本目录为准并同步修订该表 |
| [`docs/history/memory/`](../history/memory/) | 长期有效的实现约定与历史坑。编写本目录文档时必须先读 |
| [`docs/history/changes/`](../history/changes/) | 单次变更的事实记录。本目录的每次修订都应有对应的变更证据 |

## 文档清单

| 文档 | 类型 | 覆盖范围 | 状态 |
|---|---|---|---|
| [`campaign-user-stories.md`](campaign-user-stories.md) | 需求整理 + 决策记录 | 活动视角的用户故事与已确认事实 | 讨论中 |
| [`audience-user-stories.md`](audience-user-stories.md) | 需求整理 + 决策记录 | 受众侧的用户故事与已确认事实 | 讨论中 |
| [`system-boundaries.md`](system-boundaries.md) | **决策记录** | 拆成哪几个子项目、什么跨边界 | 讨论中 |
| [`business-scenarios.md`](business-scenarios.md) | 需求整理 | 五个真实场景，用来压模型 | 全部 `待确认` |

## 三类文档

本目录放三类文档，要求不同。**一份文档可以同时是前两类**（多数如此），但都不得混入第三类的内容。

| 类型 | 写什么 | 禁止出现 |
|---|---|---|
| **需求整理** | 用户故事、场景、原话。写「运营想干什么」 | 任何设计——字段、状态机、接口、存储、不变量 |
| **决策记录** | 已确认事实、能力清单、边界与归属、被推翻的判断。写「定了什么、为什么」 | **实现细节**：具体数据结构、算法实现、框架用法、部署形态 |
| **设计文档** | 领域模型。每个决策都能追溯到某条故事或某条已确认事实 | —— |

**为什么决策记录可以出现接口形状、技术倾向、目录约定**：它们是被人工确认过的**边界决策**，不是实现。例如「实验服务的接口只收 `(分流键, 一次分流的定义)`」定的是**职责边界**——它决定了这个服务能不能被独立开放；「`experimentation` 包不得依赖另外两个」定的是**依赖方向**。把这类内容一律逐出，边界就无处记录。

**但它们仍不得下沉到实现**：不写这个接口用什么协议、分桶用哪个哈希函数、包里怎么组织文件。

> 本目录曾只分「需求整理 / 设计文档」两类，并禁止需求整理出现任何技术词汇。评估器指出该规则**对本目录一份文档也不成立**——边界与能力这类内容确实需要接口形状才能表达。规则据此修订，而非把已确认的决策删掉。

尚无设计文档。**先有用户故事，再有设计**——曾经有过一份直接从业务主线推导出的设计文档，因为跳过了需求这一步而被整份删除（见 `docs/history/changes/`，原文可在 git 历史中查看）。

## 设计文档的写法约定

以下要求只适用于**设计文档**，不适用于需求整理。每份设计文档必须包含以下章节，缺一不可：

1. **状态**：草稿 / 已定稿；最后修订对应的 issue 或 PR。
2. **名词表**：本文档定义的每个概念，中文名 + 英文名 + 一句话定义。英文名是跨 repo 命名的依据。
3. **概念与关系**：概念本身、概念之间的引用方向。
4. **不变量**：必须恒真的性质。每条写成可证伪的断言，不写成愿望。
5. **状态机**：有生命周期的概念必须给出状态与合法流转，并明确非法流转。
6. **边界与非目标**：显式写出哪些职责**不**属于本模型，以及属于谁。
7. **决策记录**：本文档做出的硬决策，每条含备选方案与否决理由。**只记录选了什么和为什么，不记录讨论过程。**
8. **待澄清问题清单**：想不清楚的、当前没人在看的开放项。**不允许因为想不清楚而省略。**

### 编制纪律

- 从来源文本（业务主线、高风险清单、需求原文）编制内容时，以**来源侧**为遍历基准逐项承接，禁止复合词压缩。见 [`source-enumeration-fidelity`](../history/memory/source-enumeration-fidelity.md)。
- 承接情况必须落成**产物形态**（分项承接表），不得只写「已逐项核对」。
- 仓库内无法证明的字段名、默认值、配置不得臆造；信息不足时列入待澄清问题清单。
